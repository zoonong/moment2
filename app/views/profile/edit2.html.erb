<h1>수정 페이지</h1>

<%= form_for(Profile.edit2, url: "/profile/update/<%= @profile.id %>", method: 'post') do |f| %>

    <%= f.label :title, "글 제목" %>
    <%= f.text_field :title %><br>
    <!-- placeholder는 어떻게 써야 하는가... -->
    
    <%= f.submit "제출" %>
    
<% end %>